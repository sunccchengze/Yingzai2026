# 开场白 · 交给有识图能力的 Agent

你好，接下来由你接手做**小鹰（英仔爱心社吉祥物）的 3D 模型**。动手前，请先读完这页最上面的一段——它讲的是本仓库最重要的操作纪律，避免你把会话搞崩。

---

## 一、⚠️ 必学：快进推送（把内容送上 main，而不碰 PR）

这是本仓库铁律中最关键的一条。请**每一轮对话都记住并用它**：

```bash
# 把内容送进 main：
git push origin <你的分支>:main
```

**禁止**主动 `gh pr merge` / `gh pr close`——Arena 会在 PR 被合并或关闭后立刻关掉本会话的远程通道，之后所有 push / gh 全失败，未推送的提交会永久丢失。快进推送走纯 Git 协议，不产生「PR 合并」事件，会话通道毫发无损。

**完整操作流程：**
```bash
git status --short                      # 0. 工作区干净、提交都 commit
git fetch origin main
git merge-base --is-ancestor origin/main HEAD \
  && echo "FF 安全" || echo "先 rebase"   # 1. 自检：main 必须是你分支的祖先
git push origin <你的分支>               # 2. 先推自己的分支（绝不攒提交）
git push origin <你的分支>:main          # 3. 快进推送到 main
```

若第 1 步说「需要 rebase」：`git fetch origin main && git rebase origin/main`，解决冲突后
`git push origin <你的分支>`（自己分支可 `-f`），再 `git push origin <你的分支>:main`。
**绝不直接 `-f` 强推 main**。

> 三件套保命：① 每完成一单元立刻 commit+push，绝不攒提交；② 不主动合并/关闭 PR；
> ③ 推不上去就 `git format-patch` / `git bundle` 导 patch 存档，并如实上报，不许假装成功。

---

## 二、仓库现状（我离开时的基线，供你核对）

- 远端**只剩两条分支**：`main` 与 `arena/01a085fd-yingzai2026`，均指向整合结果。
- 当前工作分支即 `arena/01a085fd-yingzai2026`，工作区干净。
- 整合结果里**已经包含**：两个旧版小鹰 `.blend`（在 `小鹰3D模型/展翅写实版_小鹰/` 与
  `小鹰3D模型/高精绒羽版_小鹰/`）、以及挖角信/旁听制度等内容——这些**都不是**你最终要交付的模型，
  只是历史，你可参考配色与结构，但**别满足于此**。

---

## 三、你的唯一任务：看参考图，把 .blend 做到正/侧/背面高度贴合

**参考图（仓库内，必须逐张仔细看）：**
- `public/images/小鹰/小鹰正面.png`
- `public/images/小鹰/小鹰侧面.png`
- `public/images/小鹰/小鹰背面.png`

**你是本仓库里唯一能真正「看图」的 agent**，前一个会话没有视觉，只能用像素轮廓 IoU 盲调，
交出来的模型不被认可。**所以颜值/形准全靠你肉眼对照这三张图来定夺。**

**交付要求（用户原话要义）：**
1. 整体外观（轮廓、胖瘦、比例）先彻彻底底拿捏准，再去抠羽毛/毛发等细节。
2. 必须保证**正面、侧面、背面**都和参考图高度贴合、风格像。
3. 交一个 `.blend` 文件（可另附预览渲染图）。
4. 建议从参考三视图提取实际造型再建模；做完渲染正/侧/背预览，**自己对照参考图校准迭代**，
   别一次交差就完。

**参考配色（我已从像素里提取，供你直接套）：**
- 头：奶油白 ~`#F5F5F0`
- 身体/羽：巧克力棕 ~`#604818`（暗部 ~`#301800`）
- 喙/脚爪：金黄 ~`#F0A818`
- 眼/描边：近黑
- 参考图背景透明、均为清晰剪影。

---

## 四、交付后怎么办

把成品 `.blend`（与可选预览图）放回仓库合适位置并提交到你的分支，然后用第一节的
**快进推送**送上 `main`。若 `main` 已前进（你有别人没有的提交），先 rebase 再 FF，勿强推。
